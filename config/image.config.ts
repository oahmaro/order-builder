export const IMAGE_CONFIG = {
  maxWidth: 1200,
  maxHeight: 1200,
  format: 'png',
  quality: 75,
  compressionLevel: 7,
  adaptiveFiltering: true,
  palette: true,
  effort: 5,
  progressive: true,
} as const;
